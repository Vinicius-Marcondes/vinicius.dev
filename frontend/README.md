# vinicius.dev frontend

This directory is the FE-002 runtime scaffold for the new frontend.

Current state:
- Bun + Vite + React + TypeScript app exists under `frontend/`
- the legacy imported UI is preserved under `../frontend-legacy/`
- the stock Vite starter has been removed so FE-003 can add the route tree, shells, shared tokens, and slice structure cleanly

Next task:
- FE-003 establishes the `app/routes` tree, public/admin shells, and the first strict FSD skeleton
